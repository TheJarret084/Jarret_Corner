// Funkier-Pacher! bootstrap
// Good Night Abelito V2.3.3 :D
(() => {
  'use strict';

  if (!window.JSZip) {
    console.warn('JSZip no está disponible; revisa el orden de scripts.');
  }

  if (!window.FunkierCore) {
    console.warn('FunkierCore no está disponible; revisa la carga de js/modules/core.js.');
  }
})();
